name: Update Instagram Avatar

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Avatar updates disabled
        run: echo "Instagram avatar auto-update has been disabled. Current avatar remains static."
